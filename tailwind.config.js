module.exports = {
  darkMode: 'class', // Enables class-based dark mode
  theme: {
    extend: {
      fontFamily: {
        mono: ['Fira Code', 'monospace'], // Default font for all text
      },
    },
  },
  plugins: [],
};